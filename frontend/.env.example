# Leave blank in local dev — Vite's dev proxy (see vite.config.js) forwards /api to the backend.
# Set this to your deployed backend URL in production, e.g. https://api.yourapp.com/api
VITE_API_BASE_URL=
